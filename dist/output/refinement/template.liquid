{%- if issues.size == 0 -%}
### {{ checkName | capitalize }} Check Passed

**No refinement concerns found - ticket appears well-defined.**
{%- else -%}
### {{ checkName | capitalize }} Issues ({{ issues.size }})
<table>
  <thead>
    <tr>
      <th>Severity</th>
      <th>Rule</th>
      <th>Issue</th>
    </tr>
  </thead>
  <tbody>
{%- assign sorted_issues = issues | sort: 'severity' -%}
{%- for issue in sorted_issues -%}
    <tr>
      <td>{%- case issue.severity -%}
{%- when 'critical' -%}CRITICAL{%- when 'error' -%}ERROR{%- when 'warning' -%}WARNING{%- when 'info' -%}INFO{%- else -%}{{ issue.severity | upcase | escape }}{%- endcase -%}</td>
      <td><code>{{ issue.ruleId | escape }}</code></td>
      <td><div>{{ issue.message | escape }}{%- if issue.suggestion -%}
        <details><summary><strong>Suggestion</strong></summary>{{ issue.suggestion | escape }}
</details>
{%- endif -%}</div></td>
    </tr>
{%- endfor -%}
  </tbody>
</table>
{%- endif -%}

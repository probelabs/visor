diff --git a/node_modules/@probelabs/probe/cjs/agent/ProbeAgent.cjs b/node_modules/@probelabs/probe/cjs/agent/ProbeAgent.cjs
index 864cd48..9cc7c5d 100644
--- a/node_modules/@probelabs/probe/cjs/agent/ProbeAgent.cjs
+++ b/node_modules/@probelabs/probe/cjs/agent/ProbeAgent.cjs
@@ -33099,6 +33099,22 @@ async function delegate({
   if (!task || typeof task !== "string") {
     throw new Error("Task parameter is required and must be a string");
   }
+  const hasExplicitTimeout = Object.prototype.hasOwnProperty.call(
+    arguments.length > 0 ? arguments[0] : {},
+    "timeout"
+  );
+  if (!hasExplicitTimeout) {
+    const envTimeoutMs = parseInt(process.env.DELEGATION_TIMEOUT_MS || "", 10);
+    const envTimeoutSeconds = parseInt(
+      process.env.DELEGATION_TIMEOUT_SECONDS || process.env.DELEGATION_TIMEOUT || "",
+      10
+    );
+    if (!Number.isNaN(envTimeoutMs) && envTimeoutMs > 0) {
+      timeout = Math.max(1, Math.ceil(envTimeoutMs / 1000));
+    } else if (!Number.isNaN(envTimeoutSeconds) && envTimeoutSeconds > 0) {
+      timeout = Math.max(1, envTimeoutSeconds);
+    }
+  }
   const manager = delegationManager || defaultDelegationManager;
   const sessionId = (0, import_crypto2.randomUUID)();
   const startTime = Date.now();
diff --git a/node_modules/@probelabs/probe/cjs/index.cjs b/node_modules/@probelabs/probe/cjs/index.cjs
index 612e124..a769d4b 100644
--- a/node_modules/@probelabs/probe/cjs/index.cjs
+++ b/node_modules/@probelabs/probe/cjs/index.cjs
@@ -99189,6 +99189,22 @@ async function delegate({
   if (!task || typeof task !== "string") {
     throw new Error("Task parameter is required and must be a string");
   }
+  const hasExplicitTimeout = Object.prototype.hasOwnProperty.call(
+    arguments.length > 0 ? arguments[0] : {},
+    "timeout"
+  );
+  if (!hasExplicitTimeout) {
+    const envTimeoutMs = parseInt(process.env.DELEGATION_TIMEOUT_MS || "", 10);
+    const envTimeoutSeconds = parseInt(
+      process.env.DELEGATION_TIMEOUT_SECONDS || process.env.DELEGATION_TIMEOUT || "",
+      10
+    );
+    if (!Number.isNaN(envTimeoutMs) && envTimeoutMs > 0) {
+      timeout = Math.max(1, Math.ceil(envTimeoutMs / 1000));
+    } else if (!Number.isNaN(envTimeoutSeconds) && envTimeoutSeconds > 0) {
+      timeout = Math.max(1, envTimeoutSeconds);
+    }
+  }
   const manager = delegationManager || defaultDelegationManager;
   const sessionId = (0, import_crypto9.randomUUID)();
   const startTime = Date.now();
diff --git a/node_modules/@probelabs/probe/src/delegate.js b/node_modules/@probelabs/probe/src/delegate.js
index 7f7e5eb..e5fd244 100644
--- a/node_modules/@probelabs/probe/src/delegate.js
+++ b/node_modules/@probelabs/probe/src/delegate.js
@@ -363,6 +363,20 @@ export async function delegate({
 		throw new Error('Task parameter is required and must be a string');
 	}
 
+	const hasExplicitTimeout = Object.prototype.hasOwnProperty.call(arguments?.[0] ?? {}, 'timeout');
+	if (!hasExplicitTimeout) {
+		const envTimeoutMs = parseInt(process.env.DELEGATION_TIMEOUT_MS || '', 10);
+		const envTimeoutSeconds = parseInt(
+			process.env.DELEGATION_TIMEOUT_SECONDS || process.env.DELEGATION_TIMEOUT || '',
+			10
+		);
+		if (!Number.isNaN(envTimeoutMs) && envTimeoutMs > 0) {
+			timeout = Math.max(1, Math.ceil(envTimeoutMs / 1000));
+		} else if (!Number.isNaN(envTimeoutSeconds) && envTimeoutSeconds > 0) {
+			timeout = Math.max(1, envTimeoutSeconds);
+		}
+	}
+
 	// Use provided manager or fall back to default singleton
 	const manager = delegationManager || defaultDelegationManager;
 

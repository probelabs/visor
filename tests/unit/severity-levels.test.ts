// This test was testing parseAIResponse with plain schema support
// which has been removed in favor of structured schema-only approach
describe('Severity Levels', () => {
  describe('Legacy test removed', () => {
    it('should pass placeholder test', () => {
      // Test removed - parseAIResponse no longer supports plain schema
      expect(true).toBe(true);
    });
  });
});

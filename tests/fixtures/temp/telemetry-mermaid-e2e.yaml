version: '1.0'
checks:
  emit-mermaid:
    type: noop
    template:
      content: |
        # Diagram

        Here is the architecture:

        ```mermaid
        graph TD
          A[Client] --> B[API]
          B --> C[DB]

        ```
    group: diagrams
output:
  pr_comment:
    format: markdown
    group_by: check
    collapse: true

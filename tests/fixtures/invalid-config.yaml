version: "1.0"
checks:
  performance:
    # Missing 'type' field - should cause validation error
    prompt: "Review for performance issues"
    on: [pr_opened]

output:
  pr_comment:
    format: invalid_format  # Invalid format - should cause validation error
    group_by: check
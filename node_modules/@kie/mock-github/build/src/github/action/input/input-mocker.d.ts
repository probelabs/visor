import { Mocker } from "../../../github/mocker";
import { Input, InputMockerMethods } from "../../../github/action/input/input-mocker.types";
export declare class InputMocker implements Mocker, InputMockerMethods {
    private inputFromConfig;
    private currentInput;
    constructor(input: Input | undefined);
    update(key: string, value: string): void;
    delete(key: string): string;
    get(key?: string): string | Input;
    setup(): Promise<void>;
    teardown(): Promise<void>;
}

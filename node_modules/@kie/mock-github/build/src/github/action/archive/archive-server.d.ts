export declare class ArchiveServer {
    private store;
    private port;
    private server;
    private app?;
    constructor(store: string, port: string);
    private initUpload;
    private initDownload;
    start(): Promise<void>;
    stop(): Promise<void>;
}

import { Mocker } from "../../../github/mocker";
import { ArchiveArtifactsMockerMethods } from "../../../github/action/archive/archive-mocker.types";
export declare class ArchiveArtifactsMocker implements Mocker, ArchiveArtifactsMockerMethods {
    private archiveServer?;
    private store;
    private runId;
    private port?;
    private isStoreCreated;
    constructor(store: string, port?: string);
    setup(): Promise<void>;
    teardown(): Promise<void>;
    getArtifactStore(): string;
    getRunId(): string;
}

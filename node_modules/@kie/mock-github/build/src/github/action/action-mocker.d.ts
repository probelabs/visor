import { Mocker } from "../../github/mocker";
import { Action, ActionMockerMethods } from "../../github/action/action-mocker.types";
import { ArchiveArtifactsMockerMethods } from "../../github/action/archive/archive-mocker.types";
import { InputMockerMethods } from "../../github/action/input/input-mocker.types";
export declare class ActionMocker implements Mocker, ActionMockerMethods {
    private inputMocker;
    private archiveArtifactsMocker;
    constructor(action: Action | undefined, setupPath: string);
    setup(): Promise<void>;
    teardown(): Promise<void>;
    get input(): InputMockerMethods;
    get archiver(): ArchiveArtifactsMockerMethods;
}

import { SimpleGit } from "simple-git";
export declare class RepositoryBranches {
    private readonly git;
    private pushedBranches;
    private localBranches;
    constructor(git: SimpleGit);
    setPushedBranches(branches?: string[]): Promise<string[]>;
    setLocalBranches(branches?: string[]): Promise<string[]>;
    setCurrentBranch(branch?: string): Promise<string>;
}

import { Repositories } from "../../../github/repository/repository-mocker.types";
import { BranchState, FileState, RepositoryStateMethods, State } from "../../../github/repository/state/repository-state.types";
export declare class RepositoryState implements RepositoryStateMethods {
    private repositories;
    private setupPath;
    constructor(repositories: Repositories, setupPath: string);
    getState(repositoryName: string): Promise<State | undefined>;
    getForkedFrom(repositoryName: string): string | undefined;
    isFork(repositoryName: string): boolean;
    getPath(repositoryName: string): string | undefined;
    getOwner(repositoryName: string): string | undefined;
    getBranchState(repositoryName: string): BranchState | undefined;
    checkout(repositoryName: string, branch: string): Promise<void>;
    getFileSystemState(repositoryName: string): Promise<FileState[] | undefined>;
    private getFiles;
    private getFile;
}

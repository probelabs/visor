import { SimpleGit } from "simple-git";
import { GitAction } from "../../../github/repository/history/repository-history.types";
export declare class RepositoryHistory {
    private readonly git;
    private readonly repoPath;
    private readonly repofs;
    constructor(git: SimpleGit, repoPath: string);
    private push;
    private merge;
    /**
     * Reconstruct git history
     * @returns Array of objects describing what files were created and on which branch
     */
    setHistory(history?: GitAction[]): Promise<void>;
}

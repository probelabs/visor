import { CreateRepositoryFile } from "../../../github/repository/files/repository-file-system.types";
export declare class RepositoryFileSystem {
    private readonly repoPath;
    constructor(repoPath: string);
    copyFiles(files?: CreateRepositoryFile[]): Promise<boolean>;
    createFile(dest: string, data: string): Promise<void>;
    /**
     * Make sure that the destination path does not begin with REMOTE or GITIGNORE.
     * REMOTE is reserved directory to setup a local git repository
     * GITIGNORE is reserved for .gitignore file that prevents REMOTE from being pushed
     * @param dest
     * @returns true if destination path begins from REMOTE
     */
    private checkDest;
    private createDest;
    private isSubDir;
    private createTemp;
    private cleanupTemp;
}

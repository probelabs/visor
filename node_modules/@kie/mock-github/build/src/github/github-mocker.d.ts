import { ActionMockerMethods } from "../github/action/action-mocker.types";
import { EnvMethods } from "../github/env/env-mocker.types";
import { Config } from "../github/github-mocker.types";
import { Mocker } from "../github/mocker";
import { RepositoryStateMethods } from "../github/repository/state/repository-state.types";
export declare class MockGithub implements Mocker {
    private actionMocker;
    private envMocker;
    private repoMocker;
    private setupPath;
    private config;
    private setupDirCreated;
    private hasCalledSetup;
    constructor(config: string | Config, setupPath?: string);
    setup(): Promise<void>;
    teardown(): Promise<void>;
    get env(): EnvMethods;
    get repo(): RepositoryStateMethods;
    get action(): ActionMockerMethods;
    private validateConfig;
}

import { Mocker } from "../../github/mocker";
import { Env, EnvMethods } from "../../github/env/env-mocker.types";
export declare class EnvMocker implements Mocker, EnvMethods {
    private envFromConfig;
    private currentEnv;
    constructor(env: Env | undefined);
    update(key: string, value: string): void;
    delete(key: string): string;
    get(key?: string): string | Env;
    setup(): Promise<void>;
    teardown(): Promise<void>;
}

import { DataMatcherMap } from "nock/types";
import { EndpointDetails, Params } from "../../endpoint-mocker/endpoint-mocker.types";
export declare abstract class RequestMocker {
    private _endpointDetails;
    private _baseUrl;
    private _allowUnmocked;
    constructor(baseUrl: string, endpointDetails: EndpointDetails, allowUnmocked?: boolean);
    get baseUrl(): string;
    get endpointDetails(): EndpointDetails;
    get allowUnmocked(): boolean;
    protected parseParams(params?: Params): {
        path: string | RegExp;
        query: DataMatcherMap | undefined;
        requestBody: DataMatcherMap | undefined;
    };
    private extractParams;
}

import { DataMatcherMap } from "nock";
import { Header, Response } from "../../endpoint-mocker/response/abstract-response-mocker.types";
export declare abstract class ResponseMocker<T, S extends number> {
    private scope;
    private responses;
    private headers;
    private path;
    private query?;
    private requestBody?;
    private method;
    private baseUrl;
    private allowUnmocked;
    constructor(baseUrl: string, path: string | RegExp, method: string, query?: DataMatcherMap, requestBody?: DataMatcherMap, allowUnmocked?: boolean);
    matchReqHeaders(headers: Header): this;
    setResponse(responses: Response<T, S> | Response<T, S>[]): this;
    reply(response?: Response<T, S>): this;
    private createInterceptor;
}
